# Default-off vehicle-dynamics contract A/B for the native 8 m follow-stop
# candidate. CARLA produces nominal positive longitudinal acceleration above
# Apollo's MKZ default 2.0 m/s^2 during a forced trajectory replan; keep the
# measured localization truth unchanged and declare the tested simulator
# capability in Apollo's vehicle model instead.
extends: phase1_baguang_apollo_dynamic_followstop_static_planning_ready_portable_physical_steering_candidate.yaml

run:
  id: phase1_baguang_apollo_dynamic_followstop_static_planning_ready_vehicle_accel4_candidate
  profile_name: phase1_baguang_apollo_dynamic_followstop_static_planning_ready_vehicle_accel4_candidate

algo:
  apollo:
    vehicle_param:
      max_acceleration: 4.0

assist_ledger:
  can_claim_unassisted_natural_driving: false
  notes:
    - Diagnostic-only Baguang vehicle-dynamics contract A/B; inherited claim_profile keeps the existing compatibility runtime dispatch and is not a natural-driving capability claim.
    - The candidate changes only Apollo vehicle_param.max_acceleration from 2.0 to 4.0 m/s^2 for this default-off follow-stop profile.
    - CARLA localization acceleration remains measured and unmodified; no command filtering, longitudinal mapping, guard, fallback acceptance, or scene-driven ego motion is enabled.
    - Online evidence must show whether the repeated forced-replan Piecewise Jerk infeasibility and SPEED_FALLBACK disappear before this value can be promoted.
