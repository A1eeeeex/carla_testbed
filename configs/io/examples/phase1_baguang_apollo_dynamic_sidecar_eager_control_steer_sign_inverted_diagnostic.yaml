extends: phase1_baguang_apollo_dynamic_sidecar_eager_control_overlay_low_capture_paced_compat.yaml

run:
  id: phase1_baguang_apollo_dynamic_sidecar_eager_control_steer_sign_inverted_diagnostic
  profile_name: phase1_baguang_apollo_dynamic_sidecar_eager_control_steer_sign_inverted_diagnostic
  capability_profile: phase1_fixed_scene_compatibility_diagnostic_steer_sign_ab

algo:
  apollo:
    control_mapping:
      # Historical diagnostic alias retained for reproducibility. The shared
      # Baguang compatibility baseline now uses the same verified sign, so this
      # override is intentionally a no-op rather than a separate candidate.
      steer_sign: -1.0

assist_ledger:
  notes:
    - Historical Phase 1 steering-sign diagnostic profile retained as a compatibility alias after the sign contract moved into the shared Baguang baseline.
    - This is not a natural-driving or no-interference claim, and it is not permission to change steer_scale or physical mapping.
